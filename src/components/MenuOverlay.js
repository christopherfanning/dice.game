import React from 'react'

// Overlay for starting the game and some links. 
// TODO gray out the rest when overlay is active

const MenuOverlay = () => {
    return (
        <div>
            
        </div>
    )
}
//

export default MenuOverlay
